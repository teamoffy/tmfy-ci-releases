# OCI image mirrors — one line per tracked image:
#   <name> <registry/repo> <source> <sed>
#
# <name>            product suffix; releases are tagged oci-<name>/v<version>
# <registry/repo>   image repository to pull (public, anonymous)
# <source>          version authority for the image tag:
#                     <gh-owner/repo> — upstream GitHub repo whose latest
#                       release tag resolves the version (transformed by <sed>)
#                     pin:<tag>       — literal pinned tag (chart-pinned images
#                       and registries with no release source; bump in place)
#                     stack           — stack-governed product: deployable
#                       versions are declared per cloud in stacks.txt and
#                       cells emit only from those rows (a held stack emits
#                       nothing). The row exists for repo lookup and
#                       force-dispatch. Unused <sed> → "-".
# <sed>             expression applied to the release tag to get the image tag,
#                   "-" for none (e.g. k3s releases are v1.x.y+k3sN but the
#                   docker tag is v1.x.y-k3sN). Unused for pin:/stack rows.
#
# "Latest" is the upstream GitHub latest release — registry tag listings are
# unordered and paginated (ghcr caps /tags/list at 100 per page), so they
# cannot resolve latest. If a release tag has not been pushed to the registry
# yet, the image is skipped and picked up by the next run. A pin: row whose
# tag is missing fails the check.
#
# This list covers DaemonSets, CSI sidecars, cloud controllers, and upgrade
# images. The images listed by k3s itself are handled by k3s-system.
cilium                    quay.io/cilium/cilium                                     stack  -
cilium-operator           quay.io/cilium/operator                                   stack  -
cilium-envoy              quay.io/cilium/cilium-envoy                               stack  -
tetragon                  quay.io/cilium/tetragon                                   stack  -
k3s-upgrade               docker.io/rancher/k3s-upgrade                             stack  -
system-upgrade-controller docker.io/rancher/system-upgrade-controller               stack  -
kubectl                   docker.io/rancher/kubectl                                 pin:v1.36.2  -
kuberlr-kubectl           docker.io/rancher/kuberlr-kubectl                         stack  -
kured                     ghcr.io/kubereboot/kured                                  stack  -
vector                    docker.io/timberio/vector                                 stack  -
upcloud-csi               ghcr.io/upcloudltd/upcloud-csi                            stack  -
upcloud-ccm               ghcr.io/upcloudltd/cloud-controller-manager               stack  -
local-volume-provisioner  registry.k8s.io/sig-storage/local-volume-provisioner      stack  -
csi-provisioner           registry.k8s.io/sig-storage/csi-provisioner               stack  -
csi-attacher              registry.k8s.io/sig-storage/csi-attacher                  stack  -
csi-resizer               registry.k8s.io/sig-storage/csi-resizer                   stack  -
csi-snapshotter           registry.k8s.io/sig-storage/csi-snapshotter               stack  -
snapshot-controller       registry.k8s.io/sig-storage/snapshot-controller           stack  -
csi-node-driver-registrar registry.k8s.io/sig-storage/csi-node-driver-registrar     stack  -
livenessprobe             registry.k8s.io/sig-storage/livenessprobe                 stack  -
gcp-ccm                   registry.k8s.io/cloud-provider-gcp/cloud-controller-manager  stack  -
gcp-pd-csi-driver         registry.k8s.io/cloud-provider-gcp/gcp-compute-persistent-disk-csi-driver  stack  -
alicloud-ccm              registry-cn-hangzhou.ack.aliyuncs.com/acs/cloud-controller-manager  stack  -
alicloud-csi-plugin       registry.cn-beijing.aliyuncs.com/acs/csi-plugin           stack  -
